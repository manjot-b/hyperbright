#pragma once
//#include "Pickup.h"
//#include "Vehicle.h"

class PickupManager {
public:
	PickupManager();
	~PickupManager();
	void setupPickups();
	void checkActivePickups();
//onArenaPickups list
private:
//inactivePickups list
//activePickups list
};