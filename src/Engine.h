#pragma once

#include <string>
#include <glm/glm.hpp>
#include <memory>

#include "Shader.h"
#include "Mesh.h"

class Engine
{
public:
	Engine(const std::string& objPath);
	~Engine();
	
private:
	
};
